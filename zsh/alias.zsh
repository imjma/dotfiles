################################################################################
# Alias 
################################################################################
# alias gci="git commit -a -m"

if [[ `command -v nvim` > /dev/null ]]; then
  # alias vim="mvim -v"
  # alias vim="nvim"
  alias v="nvim"
fi

# copy directory
alias cpd="cp -avR"

alias fs="foreman start -f"
